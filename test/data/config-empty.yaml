golive:
  url: https://jre.apwide.net/api # fallback on cloud prod
  token: "AAA"
listeners:
  - id: golive-dev
    category:
      name: Dev
    application:
      name: app # id vs name
    autoCreate: false
