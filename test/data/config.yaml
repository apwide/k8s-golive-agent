golive:
  url: https://jre.apwide.net/api # fallback on cloud prod
  token: "AAA"
listeners:
  - namespace: golive-dev
    id: golive-dev
    selectors:
      - labels:
        golive.apwide.net/app: golive-api
        golive.apwide.net/cat: Dev
    category:
      value: "Dev" # id vs name
    application:
      label: "app" # id vs name
    autoCreate: true
